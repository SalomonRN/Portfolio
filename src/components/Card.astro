---
import { collections } from "@/content/config";

// type project = {
//   title: string;
//   description: string;
//   technologies: string;
//   img: string;
//   video: string;
//   github: string;
//   url: string;
// };
interface Props {
  project: typeof collections.projects.schema;
}

const { project } = Astro.props;

---
